<Page
    x:Class="OTP_UWP.AddSteam"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OTP_UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CommandBar Background="Transparent" DefaultLabelPosition="Right" Grid.Row="0">
            <CommandBar.Content>
                <Button Style="{StaticResource NavigationBackButtonNormalStyle}" Grid.Row="0" Name="Back" Click="Back_Click" IsEnabled="True"/>
            </CommandBar.Content>
            <AppBarButton Icon="Accept" x:Uid="Done" Name="Done" Click="Done_Click"/>
            <AppBarButton Icon="Cancel" x:Uid="Cancel" Name="Cancel" Click="Cancel_Click"/>
        </CommandBar>
        <ScrollViewer Grid.Row="1" Padding="24,24,24,24" HorizontalAlignment="Center">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock x:Uid="Add_Steam2" Style="{ThemeResource SubheaderTextBlockStyle}"  Margin="8,8,8,8"/>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition MinWidth="300"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Uid="steam_secret" Grid.Row="2"  Width="auto" Grid.Column="0" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="8,8,8,8"/>
                    <TextBox x:Name="otp_secret" Grid.Row="2" Grid.Column="1" Margin="8,8,8,8"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
